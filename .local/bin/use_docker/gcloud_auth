#!/bin/sh

envfile=$(pwd)/.env
envsetting=''
if [ -f $envfile ]; then
  envsetting="--env-file=$envfile"
fi

docker run -it $envsetting \
  --name gcloud-config google/cloud-sdk:238.0.0-alpine \
  gcloud auth login

